name: ap-cnnct
region: nyc
services:
- name: web-tester
  instance_count: 1
  instance_size_slug: basic-xxs
  github:
    repo: polymath-pat/app_pf-cnnct
    branch: main
    deploy_on_push: false
  source_dir: /
  run_command: gunicorn app:app
  http_port: 8080
